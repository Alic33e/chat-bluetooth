name: myapp2
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.3.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.4
  get: ^4.6.6
  cupertino_icons: ^1.0.6
  intl: ^0.17.0 # Move this line to the top-level dependencies section
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true